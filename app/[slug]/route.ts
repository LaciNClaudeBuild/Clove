import { db } from '@/lib/db'; import { redirect } from 'next/navigation'; export async function GET( req: Request, { params }: { params: Promise<{ slug: string }> } ) { const { slug } = await params; const result = await db.query( 'SELECT destination_url FROM links WHERE slug = $1', [slug] ); if (result.rows.length === 0) { return new Response('Link not found', { status: 404 }); } redirect(result.rows[0].destination_url); }