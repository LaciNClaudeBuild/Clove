import { db } from '@/lib/db'; export default async function Dashboard() { const linksResult = await db.query( 'SELECT id, slug, destination_url, created_at FROM links ORDER BY created_at DESC' ); const links = linksResult.rows; const clicksResult = await db.query( `SELECT link_id, COUNT(*) as total, json_agg(json_build_object('country', country, 'city', city, 'device_type', device_type, 'browser', browser, 'created_at', created_at) ORDER BY created_at DESC) as clicks FROM clicks GROUP BY link_id` ); const clicksByLink: Record<number, any> = {}; for (const row of clicksResult.rows) { clicksByLink[row.link_id] = row; } return ( <main style={{ padding: 40, fontFamily: 'sans-serif' }}> <h1>Dashboard</h1> {links.map((link) => { const stats = clicksByLink[link.id]; return ( <div key={link.id} style={{ border: '1px solid #ccc', padding: 16, marginBottom: 16, borderRadius: 8 }}> <p><strong>/{link.slug}</strong> → {link.destination_url}</p> <p>Total clicks: {stats ? stats.total : 0}</p> {stats && ( <details> <summary>Recent clicks</summary> <ul> {stats.clicks.slice(0, 10).map((c: any, i: number) => ( <li key={i}> {new Date(c.created_at).toLocaleString()} — {c.city || 'Unknown location'}, {c.country || ''} — {c.device_type} / {c.browser} </li> ))} </ul> </details> )} </div> ); })} </main> ); }