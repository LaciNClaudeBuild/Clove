'use client'; import { useState } from 'react'; export default function Home() { const [url, setUrl] = useState(''); const [customSlug, setCustomSlug] = useState(''); const [result, setResult] = useState(''); async function createLink(e: React.FormEvent) { e.preventDefault(); const res = await fetch('/api/links', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ url, customSlug }), }); const data = await res.json(); setResult(data.shortUrl || data.error); } return ( <main style={{ padding: 40, fontFamily: 'sans-serif' }}> <h1>Tender</h1> <form onSubmit={createLink}> <input type="url" placeholder="Paste a URL" value={url} onChange={(e) => setUrl(e.target.value)} required style={{ padding: 8, width: 300, display: 'block', marginBottom: 8 }} /> <input type="text" placeholder="Custom back-half (optional)" value={customSlug} onChange={(e) => setCustomSlug(e.target.value)} style={{ padding: 8, width: 300, display: 'block', marginBottom: 8 }} /> <button type="submit" style={{ padding: 8 }}> Shorten </button> </form> {result && result.startsWith('http') && ( <div style={{ marginTop: 24 }}> <p>{result}</p> <img src={`https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=${encodeURIComponent(result)}`} alt="QR code for your link" width={200} height={200} /> </div> )} {result && !result.startsWith('http') && ( <p style={{ marginTop: 16, color: 'red' }}>{result}</p> )} </main> ); }